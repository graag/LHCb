# Syrena

A set of convinience commands for LHCb ganga shell.

## Usage

In ~/.ipython-ganga/ipythonrc add na execfile call with proper path to syrena/setup.py:

`execfile ~/Soft/LHCb/syrena/setup.py`

After starting up ganga one can get interactive help by calling:

`syrena?`
